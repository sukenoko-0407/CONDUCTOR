# CONDUCTOR 0.2.1 R1 修正計画書

Status: **協議中。未承認。**
作成日: 2026-09-19
対象読者: **本設計の議論に参加していない実装担当者。**

委任表現（「適切に判断する」「必要に応じて」）を使わない。決められないものは「確認を要する」と明示する。

---

## 0. 読む順序

| 順 | ファイル | 理由 |
|---|---|---|
| 1 | 本書 | 何を変えるか |
| 2 | `CONDUCTOR_0.2.1_scale_measurement_report.md` | **全数値の根拠。実測値である** |
| 3 | `CONDUCTOR_0.2.1_specification_overview.md` の **【R1】** 箇所と **R1-1〜R1-9** | 改訂された契約 |
| 4 | `CONDUCTOR_0.2.1_spec_conformance_audit.md` | なぜこうなったか |
| 5 | `CONDUCTOR_0.2.1_independent_performance_review.md` | 性能面の詳細評価 |

**R0 の実装計画書・詳細仕様書は引き続き有効である。** 本書は差分だけを定める。

---

## 1. 前提の確認

### 1.1 実装は仕様に適合していた

適合監査で有意な逸脱は見つからなかった。**本件は実装の不具合ではなく、仕様の欠陥である。**
実装担当者に責はない。

### 1.2 性能より統計が重大である

| 問題 | 現行（本番規模・実測外挿） |
|---|---|
| L5 の wall-clock | 13.1 時間（1コア） |
| **L5 の BH 族サイズ** | **約 1,155,558** |
| **k_min（B=1000 で棄却に必要な最小件数）** | **23,088 件** |

> **L5 を 1000 倍速くしても、出力はゼロのままである。**
> **統計を先に直し、性能は後に直す。**

---

## 2. 実装しないもの

| 項目 | 理由 |
|---|---|
| **process 並列化（shard / resource token / mmap）** | 行列化と BLAS で 1 permutation が 0.1 秒になる。shard は決定性・checkpoint・BLAS thread との相互作用を増やし、**頑健性を下げる** |
| **shard checkpoint** | 修正後の L5 は B=1000 で約 2 分。**最初からやり直せる時間であることを受入条件にする** |
| **B の引き上げ** | 族縮小後は B=1000 で k_min=2.0。**引き上げ不要**（実測） |
| **特徴量重複排除を統計対策として扱うこと** | 実測で族はほぼ変わらない（1,155,558 → 1,154,917）。計算量と可読性のためだけに行う |
| **L2a / L2b / L7 の変更** | 遅いという観測がない。**測定するまで変更しない** |
| **L4 の候補 cap 緩和** | 現行 guard は有効。**並列化後も自動拡大しない** |

---

## 3. 変更一覧

| ID | 変更 | 種別 | 対象 |
|---|---|---|---|
| **M-1** | 仕事量の事前見積もり契約 | 実装 | 全 Lens + Runtime |
| **M-2** | 長時間 Node の進捗報告 | 実装 | 全 Lens + Runtime |
| **M-3** | L5 の比較単位を「文脈 vs 軸内補集合」へ | **仕様変更** | L5 |
| **M-4** | L5 の BH 族を axis ごとに分割 | **仕様変更** | L5 |
| **M-5** | 相関表の一回計算（行列化） | 実装 | L5 |
| **M-6** | Tier 1/2 特徴量の重複排除 | **仕様変更** | L5 / L1b |
| **M-7** | 較正設定 hash の記録と照合 | 実装 | Runtime |
| **M-8** | 本番設定での較正やり直し | 運用 | — |
| **M-9** | 受入基準へ計算量を追加 | 実装 | test |

---

## 4. 各変更の仕様

### M-1 仕事量の事前見積もり契約

全 Lens が次を実装する。

```python
@dataclass(frozen=True)
class WorkEstimate:
    unit_count: int               # 主要ループの反復数
    family_size: int              # BH 族の見積もり（最大の族）
    peak_memory_bytes: int        # 保持する最大構造の推定
    detail: dict[str, int]        # 内訳（contexts, features, pairs, candidates など）

def estimate_work(inputs) -> WorkEstimate: ...
```

Runtime は **Skill 起動前に** これを呼び、次を検査する。

| 検査 | 閾値 | 超過時 |
|---|---|---|
| `unit_count` | Node の `max_units`（設定値） | `needs_design_review` |
| `peak_memory_bytes` | Node の memory 予算 | `needs_design_review` |
| **`family_size`** | **`α (B+1) k_min`**（α=0.05、k_min=10） | `needs_design_review` |

**自動的に cap や閾値を変更しない。** 停止して人間へ返す。

見積もりは厳密でなくてよいが、**桁を外してはならない**。実測との比を記録し、3倍以上ずれたら警告する。

### M-2 長時間 Node の進捗報告

各 Lens は主要ループ中に一定間隔で次を Runtime へ報告する。

```text
{completed_units, total_units, elapsed_seconds}
```

- 報告間隔は **30 秒以上かつ全体の 1% 進むごと**のいずれか遅い方
- Runtime は最終報告から **10 分** 更新がない Node を `stalled` として記録する
- **自動 KILL はしない。** 人間が気づけることが目的である

### M-3 【仕様変更】L5 の比較単位

**変更前**: axis 内の全2組合せ。k=40 の軸で 780 ペア。

**変更後**: **各文脈と、その axis 内補集合**。k=40 の軸で 40 比較。

```text
axis A に属する文脈 C について
  内側 = C のメンバー
  外側 = (A に属する全文脈のメンバーの和集合) − C のメンバー
  両側で相関を計算し、|r| >= 0.30 かつ符号が逆なら候補
```

| | 変更前 | 変更後 |
|---|---|---|
| 問い | 「クラスタ1とクラスタ2で符号が逆か」 | **「この文脈は、同じ切り口の残りと符号が逆か」** |
| 比較数（k=40 軸） | 780 | **40** |
| 本番の候補数（実測外挿） | 約 1,155,558 | **約 16,647** |

**scaffold axis のように文脈が axis を覆わない場合も、和集合を外側とする。** 全化合物ではない。

Finding の `condition_id` は `{context_id}|complement` とする。`entities.context_ids` は当該文脈のみとする。

### M-4 【仕様変更】L5 の BH 族を axis 分割

**変更前**: `family_key = "L5|correlation_sign_conflict"`（単一族）

**変更後**: `family_key = f"L5|{axis_id}|correlation_sign_conflict"`

異なる切り口の検定を同じ族へ入れる統計的根拠はない。L2a が `L2a|{class}|{question}` で分割しているのと同じ考え方である。

実測で族あたり **98**、k_min **2.0** となる。

### M-5 相関表の一回計算（科学的契約を変えない）

`(context × compound)` の boolean 行列 `M` を1回作り、permutation ごとに5つの GEMM で全相関を得る。

```python
Xf  = np.isfinite(X).astype(float)
X0  = np.nan_to_num(X, nan=0.0)
yv  = y[:, None]
n   = M @ Xf
Sx  = M @ X0
Sxx = M @ (X0 ** 2)
Sy  = M @ (Xf * yv)
Syy = M @ (Xf * yv ** 2)
Sxy = M @ (X0 * yv)
num = n * Sxy - Sx * Sy
den = np.sqrt(np.maximum(n * Sxx - Sx**2, 0.0) * np.maximum(n * Syy - Sy**2, 0.0))
R   = np.where(den > 0, num / den, np.nan)
```

あわせて次を行う。

- **universe を行として実体化しない**（現行は 4,320 万 dict を list へ保持）
- **`global_r` は feature ごとに1回だけ計算する**（現行は row 生成のたび）
- **calibration の結果を screen へ再利用する**（現行は同じ permutation で二重評価）
- **BLAS の thread 数に `workers` を渡す**。process 並列は使わない

**数値同値性**: 実測で loop 方式との相対差は最大 `3.72e-10`。**許容幅 1e-9 とする。**

### M-6 【仕様変更】Tier 1/2 特徴量の重複排除

```text
1. Tier 1/2 全特徴量の pairwise |Pearson r| を計算する
2. |r| >= 0.95 を辺として連結成分を作る
3. 各成分から代表を1つ選ぶ（Tier 昇順、次に feature ID 辞書順）
4. L5 と L1b は代表特徴量だけを使う。除外分は代表への参照つきで記録する
```

**この変更の目的は計算量と可読性である。検出力の問題は M-3/M-4 が解決する。**
実測で族は 1,155,558 → 1,154,917 とほぼ変わらない。近似的に同一の記述子を5件の Finding として報告しないための措置でもある。

### M-7 較正設定 hash の記録と照合

較正値（enrichment 等）と一緒に、取得時の設定 hash を記録する。

```text
descriptor_set / 空間ID一覧 / Tier 1/2 特徴量数 / k grid / 文脈最小サイズ / B / α
```

本番実行時に不一致を検出したら **警告を出す**（停止はしない）。

### M-8 本番設定での較正やり直し

R0 の受入基準（L2b enrichment > 1.5 等）は `fast` 設定（6空間）で取得した値であり、**本番設定（9空間・2,229特徴量）に対して未検証**である。

M-3〜M-6 適用後に、**本番設定で較正を取り直す**。新しい受入基準はその値で定める。

### M-9 受入基準へ計算量を追加

R0 実装計画書 8 章へ次を追加する。

```text
- worker 1 / 2 / 64 で同一の Finding 集合を得る
- wall-clock、CPU time、平均/最大 busy CPU 数、peak RSS を記録する
- 「workers が伝播したか」ではなく「実際に消費されたか」を検証する
  判定: CPU time / wall-clock が worker 数の 50% 以上
- speedup 倍率を事前に約束しない。測定値を報告する
```

---

## 5. 実施順序

```text
段階R1-0  本番機で L5 の estimate_work だけを実行           ← コード変更なしで検証可能な部分から
段階R1-1  M-1 仕事量見積もり契約                            ← 以後の作業中に同じ事故を起こさない
段階R1-2  M-2 進捗報告
段階R1-3  M-3 + M-4（L5 の比較単位と族）                    ← 統計問題の解決。ここで止まって報告
段階R1-4  M-6 特徴量重複排除
段階R1-5  M-5 相関表の一回計算                              ← ここで初めて性能に着手
段階R1-6  M-7 較正設定 hash
段階R1-7  M-8 本番設定での較正やり直し
段階R1-8  M-9 受入基準の追加
段階R1-9  L1b / L2a / L2b / L7 の本番 baseline 測定         ← ここで初めて他 Lens の要否が決まる
```

**段階R1-3 を終えた時点で必ず止まり、報告すること。**
統計問題が解決していなければ、性能を直しても意味がない。

**段階R1-9 まで、L5 以外の Lens を変更しないこと。**

---

## 6. 受入条件

### 6.1 科学的同値性（M-5 のみ対象）

M-5 は同じ統計量を速く計算する変更である。次を満たすこと。

| 項目 | 基準 |
|---|---|
| 相関値 | 現行 loop 方式との相対差 ≤ **1e-9** |
| p 値 | 完全一致 |
| Finding 集合 | 完全一致 |

**M-3 / M-4 / M-6 は科学的契約の変更であり、同値性は要求しない。** 変更前後の候補数・Finding 数を報告する。

### 6.2 統計予算

| 項目 | 基準 |
|---|---|
| L5 の最大族サイズ | `α (B+1) k_min` = 0.05 × 1001 × 10 = **500** 以下 |
| 実測 k_min | **10 以下** |

**実測で族あたり 98 が見込まれる。** これを上回る場合は原因を報告すること。

### 6.3 性能

| 項目 | 基準 |
|---|---|
| worker 1 / 2 / 64 | **同一の Finding 集合** |
| CPU 利用 | CPU time / wall-clock ≥ worker 数の 50% |
| L5 の wall-clock | 本番機で**測定して報告する**。事前に倍率を約束しない |
| peak RSS | Node 予算内 |

**参考値**（12コア機からの線形補正、実測ではない）: B=1000 で約 1.8 分。

### 6.4 resource safety

- `estimate_work` が、**L4 の旧25万候補設定**と **L5 の R0 設定**を起動前に停止することを試験する
- 見積もりと実測の比を記録し、3倍以上のずれを警告する

### 6.5 KILL 復旧

- 修正後の L5 は最初からやり直せる時間であること（**checkpoint を実装しない**）
- L5 以外の長時間 Node については段階R1-9 の測定後に判断する

---

## 7. 実装前に確認を要する事項

| # | 項目 | 誰が決めるか | 状態 |
|---|---|---|---|
| 1 | `k_min` の値 | 設計担当 | **確定。`k_min = 10`。** 「単一の解析で最低限 Finding を10件拾いたい」との回答による |
| 2 | `max_units` / memory 予算の具体値 | 実装者が本番機で測定して提案 | 未確定（本番機測定が前提） |
| 3 | 進捗報告の間隔と stalled 判定時間 | 実装者が提案（本書の 30秒 / 10分 は初期値） | 未確定（本番機測定が前提） |
| 4 | L1b にも M-3 相当の変更が要るか | 段階R1-9 の測定後 | 未確定（測定前に変更しない） |
| 5 | Mordred 導入後の実特徴量数と候補化率 | 本番機で測定 | 未確定（本番機測定が前提） |

**#2〜#5 はいずれも本番機での測定を経ないと決められない。** 開発機では測定条件が揃わないため、机上で埋めない。
**上記以外は本書と仕様概要書 R1 で決定済みである。独断で決めないこと。**

### 7.1 文書側の完了状態

| 文書 | R1 反映 |
|---|---|
| `CONDUCTOR_0.2.1_specification_overview.md` | 済（2.5節、3.1節、L5 行、決定事項 R1-1〜R1-9） |
| `CONDUCTOR_0.2.1_scale_measurement_report.md` | 済（新規。全数値の根拠） |
| `design/discovery_lenses.md` の L5 | 済（比較単位・段階Aスクリーン・Finding 型を改訂） |
| 本書 | 済 |
| R0 実装計画書・詳細仕様書 | **変更しない。** 本書が差分を定める |

**設計文書側に未記述の項目は無い。** 残る未確定は上表 #2〜#5 のみであり、いずれも本番機での実行が前提である。

---

## 8. この修正が防ぐもの

L4 の25万候補と L5 の4,320万 universe は、**同じ欠陥から生じた**。

> **実行前に仕事量と族サイズを検査する契約が無かった。**

M-1 はこの両方を起動前に停止する。**次の Lens でも同じく停止する。**

M-7 / M-8 は別の欠陥に対応する。**較正時の設定と本番の設定が違っていた**ことを検出できるようにする。
R0 はこれを検出できず、`fast` 設定の enrichment を本番の受入基準として書いていた。
