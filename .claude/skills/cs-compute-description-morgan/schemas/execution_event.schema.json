{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://conductor.local/schemas/execution_event.schema.json",
  "title": "CONDUCTOR v4 execution event",
  "type": "object",
  "required": ["schema_version", "project", "run_id", "node_id", "capability_id", "skill_name", "status", "input_hash", "config_hash", "configuration", "artifacts", "warnings", "started_at", "finished_at"],
  "properties": {
    "schema_version": {"const": "1.0.0"},
    "project": {"type": "string", "minLength": 1},
    "run_id": {"type": "string"},
    "node_id": {"type": "string"},
    "capability_id": {"type": "string"},
    "skill_name": {"type": "string"},
    "status": {"enum": ["succeeded", "failed", "skipped"]},
    "input_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "config_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "configuration": {"type": "object"},
    "artifacts": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["type", "path"],
        "properties": {
          "type": {"type": "string", "minLength": 1},
          "path": {"type": "string", "minLength": 1},
          "sha256": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
        },
        "additionalProperties": true
      }
    },
    "warnings": {"type": "array", "items": {"type": "string"}},
    "started_at": {"type": "string", "format": "date-time"},
    "finished_at": {"type": "string", "format": "date-time"}
  },
  "additionalProperties": true
}
