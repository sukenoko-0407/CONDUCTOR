interface:
  display_name: "CONDUCTOR State Report"
  short_description: "Render a read-only CONDUCTOR State DAG report"
  default_prompt: "Use $cs-conductor-state-report to render the explicitly provided CONDUCTOR state.json as a human-readable DAG report."
