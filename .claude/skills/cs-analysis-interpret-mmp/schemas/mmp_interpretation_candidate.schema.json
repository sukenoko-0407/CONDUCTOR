{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://conductor.local/schemas/mmp_interpretation_candidate.schema.json",
  "title": "MMP Global–Local candidate",
  "type": "object",
  "required": ["category", "transform_id"],
  "properties": {
    "category": {"enum": ["variance_collapse", "cluster_specific", "direction_reversal"]},
    "transform_id": {"type": "string", "minLength": 1},
    "clustering_node_id": {"type": ["string", "null"]},
    "cluster_id": {"type": ["string", "null"]}
  },
  "additionalProperties": true
}
